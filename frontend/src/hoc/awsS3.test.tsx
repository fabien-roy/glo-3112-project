// TODO: write awsS3Connect test
// uses cases:
// - return the passed components
// - when uploadFile is called should fire S3FileUpload
// - if uploadFile went through should setState(src)
// - otherwise should log error
